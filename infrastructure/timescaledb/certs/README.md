# Certs

This directory is intentionally kept empty in the repository.

When deploying the TimescaleDB server, place the following files here:

- `ca.crt`
- `server.crt`
- `server.key`

Do **not** commit private keys or production certificates to the repository.

The required certificate files must be generated separately following the instructions in the main README.
