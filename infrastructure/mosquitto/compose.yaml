services:
  mosquitto:
    image: eclipse-mosquitto:2.1.2-alpine
    # restart: unless-stopped
    ports:
      - "8883:8883"
    volumes:
      - ./config:/mosquitto/config
      - ./data:/mosquitto/data
      - ./log:/mosquitto/log
